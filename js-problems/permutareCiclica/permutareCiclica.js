function permutareCiclica(vect, p) {
    if (p > vect.length) {
        p = p % vect.length;
    }

    for (let i = 0; i < p; i++) {

    }

    return vect;
}

// for (let i = 0; i < p; i++) {
//     let aux = vect[vect.length-1];
//     vect.splice(vect.length-1, 1);
//     vect.splice(0, 0, aux);
// }
